[pytest]
# Repo root is the import root, so tests can do `from backend.main import app`
# (matches the team's `backend.<area>` package convention).
pythonpath = .
addopts = --import-mode=importlib
norecursedirs = .venv venv node_modules .git frontend
